import { expect, test } from '../../test'

describe('users:list', () => {
    test.it('list users', () => {
        expect(1).to.equal(1);
    })
    //   test
    //   .stdout()
    //   .command(['users:list'])
    //   .it('lists users', async (ctx, done) => {
    //     const users = JSON.parse(ctx.stdout)
    //     expect(users.length).to.equal(2)

    //     done()
    //   })
})
